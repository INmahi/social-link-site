// Import configuration from the user-editable config file
import { profileConfig, socialLinksConfig } from './config';

// Export the configuration for use in the app
export const staticProfile = profileConfig;
export const staticSocialLinks = socialLinksConfig;